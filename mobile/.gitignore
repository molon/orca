node_modules/
src/terminal/terminal-webview-engine.generated.ts
src/components/pr-sidebar/mermaid-webview-engine.generated.ts
.expo/
dist/
/android/
# Generated by prebuild and kept out of git — except the one directory Xcode
# Cloud insists on finding beside the workspace. Git cannot re-include a path
# under an excluded directory, so the contents are excluded rather than `ios/`
# itself.
/ios/*
!/ios/ci_scripts/
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
web-build/
